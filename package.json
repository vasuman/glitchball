{
  "name": "glitchball",
  "version": "1.0.0",
  "description": "Football-esque game with glitching",
  "main": "index.js",
  "scripts": {
    "lint": "eslint -c .eslintrc.json src/",
    "dev": "nodemon --ignore build/ --exec uglifyjs -b -o build/out.js -- src/*.js",
    "serve": "http-server build/",
    "publish": "gh-pages -d build/"
  },
  "keywords": [
    "game",
    "football"
  ],
  "author": "Vasuman",
  "license": "ISC",
  "dependencies": {
    "glob": "^7.0.5",
    "uglify-js": "^2.7.2"
  },
  "devDependencies": {
    "eslint": "^3.3.1",
    "gh-pages": "^0.11.0",
    "http-server": "^0.9.0",
    "nodemon": "^1.10.0"
  }
}
